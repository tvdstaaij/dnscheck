{
  "name": "dnscheck",
  "version": "1.0.0",
  "description": "Check if a set of domains resolve to the desired IP address",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robin Jacobs",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.0",
    "dns": "^0.2.2"
  }
}
